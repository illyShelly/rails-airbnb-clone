<div class="container">
  <h1>Detailed flat</h1>
  <%= @flat.name %>
  <%= @flat.description %>
  <%= @flat.price %>
  <%= @flat.address %>
  <%= @flat.location %>
  <hr>
  <%= @flat.user.email %>
  <br>

<div class="row">
  <div class="col-xs-12 col-md-6">
    <%= image_path @flat.photo  %>

    <div class="card" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.2)), url('<%= cl_image_path @flat.photo, height: 500, width: 600, crop: :fit %>')">
    </div>
    <%= link_to 'Back to flats', flats_path, class: "btn btn-danger" %>
  </div>

  <div class="col-xs-12 col-md-6">
    <!-- check if current user is NOT owner -->
    <% unless current_user == @flat.user %>
      <!-- added booking form -->
      <%= render 'bookings/new' %>
      <%#= link_to 'Book flat',  new_flat_booking_path(@flat), class: "btn btn-primary" %>
      <% else %>
        <% if policy(@flat).update? %>
          <%= link_to "Update flat", edit_flat_path(@flat) %>
        <% end %>
    <% end %>
  </div>

</div>

  <%#= link_to "Delete", flat_path(@flat), class: "btn", method: :delete, data: { confirm: "Are you sure?" } %>




<!-- 1. in index -> show just others flats - not my own to book -->
<!-- 2. I am redirect when click => My property to detail flat -> where I could edit/delete/accept? -->
<!-- for displaing image <%#= cl_image_tag @article.photo, height: 300, width: 400, crop: :fill %> -->
<!-- url for http address from net -  url('<%= image_path @flat.photo %>');"> -->
